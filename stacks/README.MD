
# Python Data Structures - Stack Implementation 🚀
## 📋 Features
- **Generic Support**: Works with any data type (`int`, `str`, etc.) using Python Type 

## ⏱️ Time Complexity

| Operation | Complexity | Description |
|-----------|------------|-------------|
| **Push**  | $O(1)$*    | Amortized constant time |
| **Pop**   | $O(1)$     | Removes the last element |
| **Peek**  | $O(1)$     | Returns the top element |
| **Search**| $O(n)$     | Linear time search |

## 🧪 Testing with Pytest
This project uses **pytest** for unit testing. Every method is covered to ensure reliability.

### Running Tests:
1. Install requirements:
   ```bash
   pip install -r requirements.txt
